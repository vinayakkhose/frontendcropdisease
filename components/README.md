# Components

## Navbar
Navigation bar component that shows different links based on authentication status.

## ProtectedRoute
Wrapper component that protects routes - redirects to login if user is not authenticated.
